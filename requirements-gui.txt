wxpython >= 4.0.0
pywin32; sys_platform == 'win32'
windows_capture == 1.4.2; sys_platform == 'win32'
xlib >= 0.21; sys_platform == 'linux'
mss >= 10.1.0
PyMonCtl >= 0.92
PyOpenGL >= 3.1.9
# PyOpenGL_accelerate is not required, but it is recommended.
# If installation fails, you can ignore it.
PyOpenGL_accelerate >= 3.1.9

# https://github.com/nagadomi/wc_cuda
wc_cuda @ https://github.com/nagadomi/wc_cuda/releases/download/v0.1.2/wc_cuda-0.1.2-cp310-abi3-win_amd64.whl; sys_platform=='win32'
