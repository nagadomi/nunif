# nunif All-In-One Package for Windows

# Setup

Run `update.bat`.

Everything is installed or upgraded in this folder.
Python and Git are also included.

# TODO

Need to check error handling.
In some cases it doesn't seem to exit after an error.

Windows Defender Warning

Check if it works with LF newline code (download zip file from git)

Remove `rmdir /s /q` command but make it work properly
